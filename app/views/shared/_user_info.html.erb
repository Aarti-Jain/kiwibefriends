<% @user ||= current_user %>
<%= link_to gravatar_for(@user, size: 60), @user %>
<h1><%= @user.name %></h1>
<span><%= link_to "view my profile", @user %></span>
<span><%= pluralize(@user.microposts.count, "reviews") %></span>