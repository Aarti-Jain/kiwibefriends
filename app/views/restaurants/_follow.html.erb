<%= form_with(model: current_user.active_restaurant_relationships.build) do |f| %>
  <div><%= hidden_field_tag :restaurant_followed_id, @restaurant.id %></div>
  <%= f.submit "Follow", class: "btn btn-primary" %>
<% end %>